<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Show Detail</title>
  </head>
  <body>
    <h1>Detail Garment</h1>
    <h2><%= garment.name %></h2>
    <ul>
      <li>Lokasi: <%= garment.location %></li>
      <li>Kontak: <%= garment.contact %></li>
    </ul>
    <h2>Product</h2>
    <ul>
      <% for( let product of garment.products ) { %>
      <li>
        <a href="/products/<%= product._id  %>"
          ><%= product.name %> - <%= product.brand %></a
        >
      </li>
      <% } %>
    </ul>
    <a href="/garments">All Garment</a>
    <a href="/garments/<%= garment._id %>/edit">Edit</a>
    <form action="/garments/<%= garment._id %>?_method=DELETE" method="post">
      <button type="submit">Delete</button>
    </form>
    <a href="/garments/<%= garment._id %>/product/create">Add Product</a>
  </body>
</html>
