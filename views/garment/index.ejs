<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Garment</title>
  </head>
  <!-- <script>
    // Extract the "alert" query parameter from the URL
    const urlParams = new URLSearchParams(window.location.search);
    const alertMessage = urlParams.get("alert");

    // Display the alert message if it exists
    if (alertMessage) {
      alert(alertMessage);
    }
  </script> -->
  <body>
    <h1>Garment</h1>
    <a href="/garments/create">Add Garment</a>
    <ul>
      <% for (let garment of garments) { %>
      <li>
        <a href="/garments/<%= garment._id %>">
          <%= garment.name %> - <%= garment.location %> - <%= garment.contact %>
        </a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
